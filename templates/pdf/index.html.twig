<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Entity PDF Generator</title>
    </head>
    <body>
        <h2>{{ entity.name }}</h2>
        <hrr />

        <div>
            <p>{{ entity.description }}</p>
        </div>

        <ul>
            {% for image in images %}
                <li>
                    <img src="{{ image }}" width="100" height="100" />
                </li>
            {% endfor %}
        </ul>
    </body>
</html>
